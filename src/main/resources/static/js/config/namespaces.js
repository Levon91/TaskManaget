'use strict';

/**
 * @namespace UserManagement.app.controllers
 * controller module of the app
 */
TaskManager.app.controllers = angular.module("TaskManager.app.controllers", []);

/**
 * @namespace UserManagement.app.services
 * controller module of the app
 */
TaskManager.app.services = angular.module("TaskManager.app.services", []);

/**
 * @namespace UserManagement.app.directives
 * controller module of the app
 */
TaskManager.app.directives = angular.module("TaskManager.app.directives", []);

/**
 * @namespace UserManagement.app.filters
 * controller module of the app
 */
TaskManager.app.filters = angular.module("TaskManager.app.filters", []);

